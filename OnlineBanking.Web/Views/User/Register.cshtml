@*@{
        ViewBag.Title = "Register";
    }

    <h2>Register For Net Banking</h2>

    <form method="post" action="/User/RegisterUser">

        <p>Account Number</p>
        <input type="text"
               name="accountNumber"
               required />

        <p>User Name</p>
        <input type="text"
               name="username"
               required />

        <p>Login Password</p>
        <input type="password"
               name="password"
               required />

        <p>Transaction Password</p>
        <input type="password"
               name="transactionPassword"
               required />

        <br /><br />

        <button type="submit">
            Register
        </button>

    </form>

    @if (ViewBag.Result != null)
    {
        <hr />
        <h3>@ViewBag.Result</h3>
    }*@






@{
    ViewBag.Title = "Register For Net Banking";
}

<style>
    body {
        background: #f4f7fc;
    }

    .register-container {
        max-width: 550px;
        margin: 50px auto;
        background: white;
        border-radius: 15px;
        overflow: hidden;
        box-shadow: 0 5px 20px rgba(0,0,0,0.15);
    }

    .register-header {
        background: linear-gradient(135deg, #0d6efd, #0b5ed7);
        color: white;
        text-align: center;
        padding: 25px;
    }

    .register-body {
        padding: 30px;
    }

    .form-group {
        margin-bottom: 18px;
    }

        .form-group label {
            display: block;
            margin-bottom: 6px;
            font-weight: 600;
            color: #333;
        }

    .form-control {
        width: 100%;
        padding: 12px;
        border: 1px solid #ddd;
        border-radius: 8px;
        font-size: 14px;
    }

    .btn-register {
        width: 100%;
        background: #0d6efd;
        color: white;
        border: none;
        padding: 14px;
        border-radius: 8px;
        font-size: 16px;
        font-weight: bold;
        cursor: pointer;
    }

        .btn-register:hover {
            background: #0b5ed7;
        }

    .result-box {
        margin-top: 20px;
        padding: 15px;
        border-radius: 8px;
        background: #d4edda;
        color: #155724;
        border: 1px solid #c3e6cb;
    }

    .home-link {
        display: block;
        text-align: center;
        margin-top: 20px;
        text-decoration: none;
        color: #0d6efd;
        font-weight: 600;
    }
</style>

<div class="register-container">

    <div class="register-header">
        <h2>🏦 Net Banking Registration</h2>
        <p>Create your online banking credentials</p>
    </div>

    <div class="register-body">

        <form method="post" action="/User/RegisterUser">

            <div class="form-group">
                <label>Account Number</label>
                <input type="text"
                       name="accountNumber"
                       class="form-control"
                       required />
            </div>

            <div class="form-group">
                <label>User Name</label>
                <input type="text"
                       name="username"
                       class="form-control"
                       required />
            </div>

            <div class="form-group">
                <label>Login Password</label>
                <input type="password"
                       name="password"
                       class="form-control"
                       required />
            </div>

            <div class="form-group">
                <label>Transaction Password</label>
                <input type="password"
                       name="transactionPassword"
                       class="form-control"
                       required />
            </div>

            <button type="submit" class="btn-register">
                Register
            </button>

        </form>

        @if (ViewBag.Result != null)
        {
            <div class="result-box">
                <strong>✅ @ViewBag.Result</strong>
            </div>
        }

        <a href="/Home/Index" class="home-link">
            ← Back To Home
        </a>

    </div>

</div>